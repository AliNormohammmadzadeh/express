{{!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/css.css">
    <title>Item Description</title>
</head>
<body>
        <header>
        <div class="cont">
                {{#with owners}}
                <h1>Wellcome to {{firstname}} {{lastname}} shop</h1>
                {{/with}}
        </div>
        <nav>
            <ul class="nav_links"> 
                <li><a href="/shop">Main Page</a></li>
                <li><a href="/add">Add items</a></li>
                <li><a href="/userinfo">Edit profile</a></li>
                
            </ul>
        </nav>
    </header> --}}
    <div class="flexk">
        <div class="kala1">
            <p>Esme kala : {{item.title}}</p>
            <p>gheymate kala : {{item.price}} $</p>
            <p>tozihate  kala : {{item.description}}</p>
            <img src="{{item.img}}" alt="{{item.title}}">
        </div>
        <div>
            <h1>Editing items</h1>
            {{!-- "?_method=PUT" after item_id " enctype="multipart/form-data" ==> for multer    --}} 
            <form action="/shop/{{item._id}}?_method=PUT" method="POST" >
                <ul class="form-style-2">
                    <li><label>Title<span class="required">*</span></label><input type="text" name="title" class="field-divided" placeholder="Title" /> 
                    <li>
                        <label>Mablagh <span class="required">*</span></label>
                        <input type="number" name="price" class="field-long" />
                    </li>
                    <li>
                        <label>Description<span class="required">*</span></label>
                        <textarea name="description" id="description" class="field-long field-textarea"></textarea>
                    </li>
                    <li>
                        <input type="text" name="img" placeholder="Image URL" required>
                    </li>
                    <li>
                        <input type="submit" value="Edit Item" />
                    </li>
                </ul>
            </form> 
        </div>
    </div>
{{!-- 
</body>
</html> --}}